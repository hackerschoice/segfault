base {
	//log_debug = off;
	//log_info = off;
        log = "stderr";
        daemon = off;
        redirector = iptables;
}

redsocks {
        local_ip = 127.0.0.1;
        local_port = 1040;
        ip = 127.0.0.1;
        port = 1080;
        type = socks5;
}
